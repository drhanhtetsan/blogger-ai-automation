# Workflows Folder

This folder documents CI/CD workflow intent.

- `.github/workflows/ci.yml` runs static quality checks and tests.
- `.github/workflows/publish.yml` runs the automation manually or on schedule.
